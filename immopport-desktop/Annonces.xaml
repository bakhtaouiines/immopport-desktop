<UserControl x:Class="immopport_desktop.Annonces"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:immopport_desktop"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView Margin="10" Name="lvUsers">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="120" DisplayMemberBinding="{Binding PropertyId}" />
                    <GridViewColumn Header="Titre" Width="120" DisplayMemberBinding="{Binding Titre}" />
                    <GridViewColumn Header="Adresse" Width="50" DisplayMemberBinding="{Binding Address}" />
                    <GridViewColumn Header="Prix" Width="150" DisplayMemberBinding="{Binding Prix}" />
                    <GridViewColumn Header="Actions" Width="150"  />

                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
